# კედრის ქუჩა — პროექტის მეხსიერება

## ჩატების ლოგირება (ავტომატური)

ამ პროექტში **ყოველი სესია ავტომატურად ლოგდება**. ორი დონეა:

| დონე | ფაილი | რა არის |
|---|---|---|
| ზედა | `docs/chat-logs/INDEX.md` | ყველა წინა ჩატი, თითო ერთი სტრიქონი |
| ქვედა | `docs/chat-logs/<თარიღი>-<sessid>.md` | კონკრეტული სესიის სრული დეტალები |

**წესი ახალი სესიისთვის:** ინდექსი ავტომატურად ჩაგივარდება კონტექსტში სესიის
დაწყებისას. **წინასწარ ყველა დეტალურ ფაილს ნუ წაიკითხავ** — მხოლოდ იმას, რომლის
თემაც მიმდინარე ამოცანას ეხება.

### როგორ მუშაობს

სამი hook (`.claude/settings.json`):

- **`SessionStart`** → `chatlog-inject.sh` — კითხულობს `INDEX.md`-ს და სესიის
  კონტექსტში აგდებს
- **`SessionEnd`** → `chatlog-write.sh` — ტრანსკრიპტიდან ააგებს ქართულ ლოგს
- **`PreCompact`** → `chatlog-write.sh` — იგივე, კომპაქტამდე checkpoint-ად

`chatlog-write.sh` თავს ფონურ პროცესად იმეორებს (სესიის დახურვამ არ მოკლას),
`chatlog-extract.jq`-ით ტრანსკრიპტიდან იღებს მხოლოდ დიალოგს (tool-ების ნაგავის
გარეშე, მაგრამ `AskUserQuestion`-ის პასუხებით), შემდეგ `claude -p --model sonnet`
ააგებს შეჯამებას. `chatlog-index.sh` ყოველ ჯერზე თავიდან ააგებს `INDEX.md`-ს
ლოგების frontmatter-იდან.

ლოგის იდენტიფიკატორი = სესიის id-ის პირველი 8 სიმბოლო. ერთი სესია = ერთი ფაილი
(PreCompact-ის შემდეგ SessionEnd იმავე ფაილს გადააწერს).

### ხელით გაშვება

```sh
.claude/hooks/chatlog-write.sh <transcript.jsonl> <პროექტის-გზა>
.claude/hooks/chatlog-index.sh <პროექტის-გზა>          # მხოლოდ ინდექსის აღდგენა
```

დიაგნოსტიკის ლოგი: `$TMPDIR/chatlog-hook.log`

### ლოგები ლოკალურია

`docs/chat-logs/` **git-ში არ არის** (`.gitignore`). შეჯამებას სკრიპტი
ტრანსკრიპტიდან ავტომატურად აგებს, ფილტრის გარეშე — თუ სესიაში
მაცხოვრებლის სახელი ან ნომერი ახსენე, ის შეჯამებაში მოხვდება, repo კი
საჯარო იქნება. ფაილები დისკზე რჩება და hook-ები უცვლელად მუშაობს.

### რა არ ილოგება

- 400 ბაიტზე მოკლე სესია (მაგ. მხოლოდ მისალმება)
- საიდჩეინები (subagent-ების შიდა დიალოგი)
- ჩვენივე გამოძახებული `claude -p` — `CHATLOG_RUNNING` ცვლადი რეკურსიას კეტავს

## პროექტის ფაილები

| ფაილი | შიგთავსი |
|---|---|
| `კედრის_ქუჩა_ხელმოწერები.xlsx` | 71 ჩანაწერი, 2 ფურცელი (ხელმოწერები, ნაკვეთები) |
| `კედრის_ქუჩა_ნაკვეთები.geojson` | 66 პოლიგონი |
| `კედრის_ქუჩა_რუკა.png` | 22 MB სკანი — git-ში ატვირთვამდე `.gitignore`-ში |
| `docs/2026-08-20-chat-log.md` | ხელით დაწერილი ლოგი ავტომატიზაციამდე |
