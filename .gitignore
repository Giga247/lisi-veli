# 22 MB რასტრული რუკა — repo-ს ამძიმებს, GitHub-ს 100 MB ლიმიტი აქვს
*.png

# იმპორტის შედეგები — Sheet-ში იდება, repo-ში არა
build/

# სისტემური
.DS_Store
__pycache__/
*.pyc

# ── პერსონალური მონაცემები — არასოდეს არ უნდა მოხვდეს საჯარო repo-ში ──
# GitHub Pages უფასო ანგარიშზე საჯარო repo-ს მოითხოვს. ამ ფაილებში
# 71 ოჯახის სახელი, გვარი და ტელეფონია — ბაზა Google Sheet-ში ცხოვრობს,
# repo-ში მხოლოდ კოდი უნდა იყოს.
*.xlsx
*.xls
*.docx
*.doc
*.geojson
*.csv

# ── ჩატების ლოგები — ლოკალური, არა საჯარო ──────────────────────────
# `chatlog-write.sh` ტრანსკრიპტიდან მთელ დიალოგს იღებს და შეჯამებას
# ავტომატურად წერს — რედაქტირების გარეშე. თუ სესიაში მაცხოვრებლის
# სახელი ან ნომერი ახსენე, ის შეჯამებაში მოხვდება. ლოგები Drive-ზე
# ისედაც სინქრონდება; საჯარო repo-ს ისინი არ სჭირდება.
docs/chat-logs/
docs/*-chat-log.md
.claude/handoff-*.md

# გამონაკლისი: ნახაზის სტატიკური მონაცემი. აქ პირადი ველი არ არის —
# მხოლოდ საკადასტრო კონტურები, კოდები, მისამართის ნომრები და ქუჩები,
# ანუ ის, რაც საჯარო საკადასტრო რუკაზე ისედაც ხელმისაწვდომია.
# იხ. docs/superpowers/specs/2026-08-21-lisi-veli-gegma-design.md §4.2
!data/plan.geojson

# იგივე კლასის მონაცემი, რაც ზემოთ — გეგმის გვერდის კონტურები, კოდები
# და მისამართები. მფლობელი, ტელეფონი და სახელი აქ არ ხვდება.
!data/plan-page.json

# ── გეგმის გვერდის ნაშენი ─────────────────────────────────────────
# `tools/plan_page/build.py`-ს გამოსატანი. `data/plan-page.json` საიტს
# სჭირდება და git-შია (იხ. ქვემოთ გამონაკლისი), ცალკე მდგომი HTML კი
# ყოველ აგებაზე თავიდან იქმნება — repo-ს არ სჭირდება.
tools/plan_page/kedri-plan.html

# ისტორიის სარეზერვო ასლი (PII-ს შეიცავს) — ლოკალური
backup/
*.bundle

# Supabase-ის საიდუმლოებები — არასოდეს git-ში
.env.local
.env
supabase/.temp/
